import { View, Text, SafeAreaView } from 'react-native';

export function Perfil() {
      return(
            <SafeAreaView>
                  <Text>Perfil</Text>
            </SafeAreaView>
      );
}